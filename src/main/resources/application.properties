# MySQL connection properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/mylogistics?useSSL=true
spring.datasource.username=root
spring.datasource.password=root

# MySQL connection properties (create-drop, update, etc)
spring.jpa.hibernate.ddl-auto = create-drop

# Comment this in production
spring.jpa.show-sql = true

# Hibernate SQL dialect
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql = true

server.port = 8181
server.error.include-message = always

spring.devtools.restart.enabled = true
spring.devtools.livereload.enabled = true

#Rest Application base url
server.servlet.context-path = /api/v1

#Spring Security default access login
#spring.security.appUser.name = admin
#spring.security.appUser.password = admin

